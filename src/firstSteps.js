/**
 * Ugly code just for the training
 * Returns true if number parameter is even
 * @param number
 * @returns {boolean}
 */
function isEven(number) {
  let checker = false;
  if (number % 2 === 0){
    checker = false;
  }
  return checker
}


module.exports = {
  isEven
};